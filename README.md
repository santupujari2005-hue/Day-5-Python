# 📘 Python Practice – Sets & Dictionaries

This repository contains practice programs covering **Python Sets and Dictionaries** concepts.

---

## 🔹 Topics Covered

### ✅ Sets in Python
- Creating sets
- Removing duplicate values
- Empty set creation using `set()`
- Set methods:
  - `add()`
  - `remove()`
  - `clear()`
  - `pop()`
  - `union()`
  - `intersection()`

### ✅ Dictionaries in Python
- Creating dictionary (key-value pairs)
- Storing meanings using dictionary
- Counting unique subjects using set
- Taking user input and storing in dictionary
- Updating dictionary using `update()`

---

## 🔹 Key Learning Points

- Sets store only unique elements.
- Sets are unordered and do not support indexing.
- `{}` creates an empty dictionary, not a set.
- Dictionary keys must be unique.
- Use `set()` to create an empty set.
- Use `update()` to add values in dictionary.

---

## 🚀 Learning Goal

Strengthening Python fundamentals before moving to Data Structures & Algorithms (DSA).

---

## 👨‍💻 Author

Santosh Pujari  
CSE Student | Python Learner | Future Software Engineer 💻🔥
